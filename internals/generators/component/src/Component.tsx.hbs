import React from 'react';
import { {{ properCase name }}Root } from './components';
import { I{{ properCase name }}Props } from './types';

const {{ properCase name }} = React.forwardRef<HTMLDivElement, I{{ properCase name }}Props>(
  (props, ref) => {
    const { children, ele, ...rest } = props;
    return (
      <{{ properCase name }}Root ref={ref} role="{{ name }}" {...rest}>
        {ele ? React.cloneElement(ele, {}, children) : children}
      </{{ properCase name }}Root>
    );
  }
);

{{ properCase name }}.defaultProps = {
  
};

{{ properCase name }}.displayName = '{{ properCase name }}';

export { {{ properCase name }} };
