import { css } from '@tidy-ui/commons';
import { ICodeProps } from './types';

const IconStyle = css<ICodeProps>`
  position: absolute;
  top: 0.5rem;
  right: 0.5rem;
  height: 1.8rem;
  width: 1.8rem;
  padding: 0.25rem;
  border-radius: 4px;
`;

export { IconStyle };
