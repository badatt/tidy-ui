{
  "name": "@tidy-ui/types",
  "version": "0.0.1",
  "scripts": {
    "bootstrap": "lerna bootstrap --use-workspaces",
    "build": "yarn clean && tsc -b",
    "clean": "tsc --build --clean",
    "yalc:publish": "yarn build && yalc publish",
    "yalc:push": "yarn build && yalc push"
  },
  "dependencies": {},
  "peerDependencies": {},
  "devDependencies": {
    "typescript": "^4.4.4"
  },
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "description": "Tidy UI's 'types' module component library",
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "dist"
  ]
}
