{
  "name": "@tidy-ui/card",
  "version": "0.0.3",
  "scripts": {
    "bootstrap": "lerna bootstrap --use-workspaces",
    "build": "yarn clean && tsc -b",
    "clean": "tsc --build --clean",
    "yalc:publish": "yarn build && yalc publish",
    "yalc:push": "yarn build && yalc push"
  },
  "dependencies": {
    "clsx": "1.1.1",
    "styled-components": "5.0.0"
  },
  "peerDependencies": {
    "react": "^16.0.0"
  },
  "devDependencies": {
    "@types/react": "^17.0.34",
    "react": "^16.0.0",
    "typescript": "^4.4.4"
  },
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "description": "Tidy UI's Card module component library",
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "dist"
  ]
}
